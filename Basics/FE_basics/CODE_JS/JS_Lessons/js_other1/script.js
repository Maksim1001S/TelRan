answer.onclick = function () {
    var ans = document.querySelector("#forUser").value
    alert(ans);
}

let item = document.querySelectorAll('.item')

for (let i = 0; i < item.length; i++) {
    item[i].insertAdjacentHTML('afterbegin',)
    
}